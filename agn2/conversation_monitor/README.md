# Conversation Monitor

Conversation Monitor displays AGN message streams and replayable event records.

## Commands

```bash
npm install
npm run dev
```

The monitor reads local runtime data and does not store provider credentials.
